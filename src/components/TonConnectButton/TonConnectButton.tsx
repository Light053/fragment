import { Box } from "@mui/material";
import { TonConnectButton } from "@tonconnect/ui-react";

export const TonConnectButtonComponent = () => {
  return (
    <Box>
      <TonConnectButton />
    </Box>
  );
};
